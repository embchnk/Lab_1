language: python
python:
  - "3.5"

#command to install dependencies
install: "pip install -r requirements.txt"

#command to run tests
script: 
        "cd Lab_03 && python3.5 -m unittest tests/test_ticTacToeVsComp.py"